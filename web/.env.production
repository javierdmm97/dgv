# Production Firebase Web config, baked into the build (incl. CI).
# These are PUBLIC client identifiers (shipped in every browser bundle), NOT secrets —
# access is controlled by Firestore security rules, not by hiding these values.
# Committed on purpose so CI builds need no extra configuration.
VITE_FIREBASE_API_KEY=AIzaSyCerDRDWf_xyAWPA-hvKcGqeA93E4Z6ajA
VITE_FIREBASE_AUTH_DOMAIN=dgv-project-7824a.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=dgv-project-7824a
VITE_FIREBASE_APP_ID=1:529384583376:web:3476823ac81884b34aaed5
