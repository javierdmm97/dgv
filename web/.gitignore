node_modules
dist
dist-ssr
*.local
.env
# .env.production holds PUBLIC Firebase client keys and MUST be committed so CI
# builds get the config (overrides the root .gitignore's `.env.*` rule).
!.env.production
.firebase
.DS_Store

# Build artifacts (tsc -b emits these from vite.config.ts / project refs)
*.tsbuildinfo
vite.config.js
vite.config.d.ts
